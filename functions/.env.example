# Functions runtime env vars
# Set these in Cloud Functions (recommended: Secrets for API keys).

# Resend
RESEND_API_KEY=""
# This MUST be a verified sender in Resend (domain or single sender).
# You can use a plain email (e.g. contato.soraiatarot@gmail.com) or a display-name format.
RESEND_FROM_EMAIL="Soraiá Tarot <contato.soraiatarot@gmail.com>"

# Optional: if the Gmail `RESEND_FROM_EMAIL` isn't verified/allowed, the function can retry with a fallback verified sender.
# Example values commonly used during setup: onboarding@resend.dev
RESEND_FALLBACK_FROM_EMAIL=""

# WhatsApp number used in email CTA
WHATSAPP_BUSINESS_NUMBER="5511999999999"
